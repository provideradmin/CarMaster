{% extends 'base.html.twig' %}

{% block title %}Create Car{% endblock %}

{% block body %}
    <h1 class="my-4">Create Car</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Create</button>
    <a href="{{ path('list_cars') }}" class="btn btn-secondary">Back to list</a>
    {{ form_end(form) }}
{% endblock %}
